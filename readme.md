### One click to install xray

just run as 

```bash
wget https://raw.githubusercontent.com/flyzstu/one-key/master/install.sh 
chmod +x install.sh
./install.sh --install --mirror cloudlare --arch 64
```
